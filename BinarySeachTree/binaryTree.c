#include "binaryTree.h"

struct binTree{
    binTree *left;
    binTree *right;
    int val;

}arbol;

binTree createTree(binTree left,binTree right,int val){
    binTree *t=malloc(sizeof(struct binTree));
    a
    

}